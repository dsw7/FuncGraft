#pragma once

#include <string>

namespace core {
namespace reporting {
void print_prompt(const std::string &prompt);
void print_code_being_targeted(const std::string &code);
} // namespace reporting
} // namespace core
